<!DOCTYPE html>
<html class="h-full bg-slate-200">
  <head>
    <title>LightningMarineServices</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <!-- <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css"> -->
    <%#= stylesheet_link_tag "app" %>
    <%#= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%#= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <%= stylesheet_link_tag "users" if controller_path.include? "users" %>

    <%= stylesheet_link_tag "app" %>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "application", defer: true %>
  </head>
  <body class="h-full">
    <div class="min-h-full">
      <div class="bg-gray-800 pb-32">
        <!-- Navigation -->
        <div id="top-level-nav-wrapper">
          <!-- NOTE  / TODO : Add admin links to navs! -->
          <section id="stacked-shell-nav-section">
            <%# tmp_search_input_placeholder_text = "Sorry, search not availible on the page" %>
            <% tmp_search_input_placeholder_text = "Search" %>
            <%= render partial: "layouts/stacked_shell/nav", locals:{
              search_input_placeholder_text: tmp_search_input_placeholder_text,
            } %>
          </section>
        </div>

        <!-- stacked_shell/flash_messages to be appended to this div -->
        <div id="flashes">
        <%= render "shared/flash_messages" %> <!-- old flashes -->
        </div>

        <!-- Page title & actions -->
        <div id="top-level-header-wrapper" class="py-6 sm:py-12 ">
          <%= render partial: "layouts/stacked_shell/header" %>
        </div>
      </div>

      <div class="-mt-32 relative">
        <div class="rounded-md mx-auto max-w-7xl px-4 pb-12 sm:px-6 lg:px-8">
          <div class="rounded-md bg-white">
            <div id="top-level-secondary-nav-wrapper">
              <section id="stacked-shell-secondary-nav-section">
                <%= render partial: "layouts/stacked_shell/secondary_nav" %>
              </section>
            </div>

            <div class="flex-1 mt-4 md:mt-6 pb-8">
              <div class="mx-auto max-w-8xl px-4 sm:px-6 lg:px-8">
                <main id="main">
                  <section id="stacked-shell-main-section">
                    <%= yield %>
                  </section>
                </main>
              </div>
            </div>

          </div>
        </div>

        <%= turbo_frame_tag :modal %>

        <div class="after-main -mt-3 rounded-md mx-auto max-w-7xl pb-12 px-4 sm:px-6 lg:px-8">
          <section id="stacked-shell-main-section-under">
            <%= yield :main_content_under if content_for?(:main_content_under) %>
          </section>
        </div>
      </div>
    </div>

  </body>
</html>
