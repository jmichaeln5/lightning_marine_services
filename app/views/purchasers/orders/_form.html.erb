<%= form_with model: [:purchaser, @order] do |form| %>
<%#= form_with model: [:purchaser, :order] do |form| %>
<%#= form_with model: [:purchaser, @order] do |form| %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
