<% content_for :title, "#{Purchaser.all.count} Ships" %>

<%= render 'shared/tab_table_options' %>
<%= render 'shared/new_modal' %>
<%= render 'shared/top_table_header', total_pages: @purchasers_resource.total_pages %>
<%= render partial: 'purchasers/index', locals: { purchasers: @purchasers_resource} %>
<%= render "shared/pagination/pagination_buttons", total_pages: @purchasers_resource.total_pages %>
