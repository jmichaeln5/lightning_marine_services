<% content_for :title, "#{Purchaser.all.count} Ships" %>

<%= render 'shared/tab_table_options' %>
<%= render 'shared/new_modal' %>

<%= render partial: "shared/purchasers_and_vendors_shared/index", locals: {resources: @purchasers} %>

<%= render "shared/pagination_buttons", total_pages: @purchasers.total_pages %>
