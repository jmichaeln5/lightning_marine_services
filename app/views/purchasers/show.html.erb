<% content_for :title, "Ship: #{@purchaser.name}" %>
<% content_for :subtitle, "#{@sorted_purchaser_orders.ids.count} Orders" %>

<%= render 'shared/tab_table_options' %>
<%= render 'shared/new_order_on_show_modal' %>

<%= render 'shared/top_table_header', resource: @orders %>
<%= render partial: "show", locals: {resource: @orders} %>
<%= render "shared/pagination/pagination_buttons", total_pages: @orders.total_pages %>
