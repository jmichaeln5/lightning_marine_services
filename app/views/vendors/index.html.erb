<% content_for :title, "#{Vendor.all.count} Vendors" %>

  <%= render 'shared/tab_table_options' %>
  <%= render 'shared/new_modal' %>

  <%= render 'shared/top_table_header', resource: @vendors %>
  <%= render partial: "index", locals: {vendors: @vendors} %>
  <%= render "shared/pagination/pagination_buttons", total_pages: @vendors.total_pages %>
