<% content_for :title, 'Dashboard' %>
<%= render 'shared/under_header_card_options' %>
<div class="text-center mt-20">
  <div class="card">
    <div class="p-4">
    <p class="text-3xl"> Welcome <%=current_user.first_name if current_user %> </p>

      <%= "#{Order.all.count} Total Orders" %>
      <br>
      <%= "#{@unarchived_orders.unarchived.all.count} Unarchived Orders" %>
      <br>
      <%= "#{@archived_orders.count} Archived Orders" %>

    </div>
  </div>
</div>
