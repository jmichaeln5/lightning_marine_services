<% content_for :title, "Search Page" %>
<% content_for :subtitle, "#{Order.all.count} Orders" %>
<% content_for :subtitle_two, "#{ Order.unarchived.count} Unarchieved Orders" %>
<% content_for :subtitle_three, "#{Order.archived.count} Archieved Orders" %>

<%= render 'shared/tab_table_options' %>
<br>
<%= render 'shared/top_table_header', total_pages: @resource.total_pages %>
<%= render partial: 'orders/index', locals: { orders: @orders} %>
<%= render "shared/pagination/pagination_buttons", total_pages: @resource.total_pages %>
